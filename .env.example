# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=henry_capstone
POSTGRES_PORT=5439

# Langfuse Database (uses same PostgreSQL instance)
LANGFUSE_DB=langfuse

# ChromaDB Configuration
CHROMA_PORT=8000
CHROMA_AUTH_CREDENTIALS=
CHROMA_AUTH_PROVIDER=
CHROMA_TELEMETRY=false

# Langfuse Configuration
LANGFUSE_PORT=3000
LANGFUSE_URL=http://localhost:3000
# IMPORTANT: Generate secure secrets for production!
# You can use: openssl rand -base64 32
LANGFUSE_SECRET=changeme-generate-a-secure-secret
LANGFUSE_SALT=changeme-generate-a-secure-salt
LANGFUSE_TELEMETRY=true
LANGFUSE_EXPERIMENTAL=false
