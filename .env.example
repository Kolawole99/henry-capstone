# OpenAI API Configuration
OPENAI_API_KEY=sk-your-key-here
# Optional: Set this if using a custom endpoint (e.g., local LLM or proxy)
OPENAI_BASE_URL=https://openrouter.ai/api/v1

# Database Configuration
DATABASE_URL=postgresql://postgres:postgres@localhost:5439/henry_capstone

# PostgreSQL Configuration
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=henry_capstone
POSTGRES_PORT=5439

# Langfuse Database (uses same PostgreSQL instance)
LANGFUSE_DB=langfuse

# ChromaDB Configuration
CHROMA_PORT=8000
CHROMA_AUTH_CREDENTIALS=
CHROMA_AUTH_PROVIDER=
CHROMA_TELEMETRY=false

# Langfuse Configuration
LANGFUSE_HOST=https://cloud.langfuse.com
LANGFUSE_SECRET_KEY=changeme-generate-a-secure-secret-key
LANGFUSE_PUBLIC_KEY=changeme-generate-a-secure-public-key
